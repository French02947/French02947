def cut_off(num):
    # Conver num to a string
    num_str = str(num)

    # Create an empty list
    result = []

    for i in range(len(num_str)):
        result.append(num_str[:i + 1])

    return result
